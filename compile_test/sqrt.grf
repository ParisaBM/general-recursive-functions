add = rec(proj 0 1, comp(suc, proj 1 3))
mul = rec(0, comp(add, proj 1 3, proj 2 3))
square = comp(mul, proj 0 1, proj 0 1)
pred = rec(0, proj 0 2)
sub = rec(proj 0 1, comp(pred, proj 1 3))
next_square = comp(square, suc)
gt = comp(sub, proj 0 2, comp(suc, proj 1 2))
main = min(comp(gt, comp(next_square, proj 0 2), proj 1 2))